@model Document
@{
    ViewData["Title"] = "Super Admin";
}
<!-- Page Breadcrumb -->
<div class="page-breadcrumbs">
    <ul class="breadcrumb">
        <li>
            <i class="fa fa-home themeprimary"></i>
            @if (ViewBag.RoleID == 1)
            {
                <a href="/SuperAdmin/Index">Dashboard</a>
            }
            else if (ViewBag.RoleID == 2)
            {
                <a href="/ClientAdmin/Index">Dashboard</a>
            }
            else if (ViewBag.RoleID == 3)
            {
                <a href="/Manager/Index">Dashboard</a>
            }
            else
            {
                <a href="/Employee/I9categories">Dashboard</a>
            }
        </li>

        <li class="active">My Documents</li>
    </ul>
</div>
<!-- /Page Breadcrumb -->
<!-- Page Header -->
<div class="page-header position-relative">
    <div class="header-title">
        <h1 class="themeprimary">
            My Documents
        </h1>
    </div>
    <!--Header Buttons-->
    <div class="header-buttons">
        <a class="sidebar-toggler" href="#">
            <i class="fa fa-arrows-h"></i>
        </a>
        <a class="refresh" id="refresh-toggler" href="">
            <i class="glyphicon glyphicon-refresh"></i>
        </a>
        <a class="fullscreen" id="fullscreen-toggler" href="#">
            <i class="glyphicon glyphicon-fullscreen"></i>
        </a>
    </div>
    <!--Header Buttons End-->
</div>
<!-- /Page Header -->
<!-- Page Body -->
<div class="page-body">
    <!-- Share Document -->
     <div class="col-md-12 col-lg-12 col-sm-12 col-xs-12 no-padding-left no-padding-right" id="popup" style="display:none; padding:50px;">
        <div class="form-group col-md-12 col-lg-12 col-sm-12 col-xs-12 no-padding-left" style="margin-bottom:10px">
            <h4 style="font-size:16px;"><b>Document Title : </b> <label style="font-size: 15px;color:black"> </label></h4>

        </div>
        <div class="form-group col-md-3 col-lg-3 col-sm-3 col-xs-12 no-padding-left">
            <label for="inputEmail3" class="control-label">Share To (User Name) :</label>
           
            <select multiple="" style="width: 100%;" tabindex="-1" class="form-select form-control" aria-hidden="true"  >
                <option>raju@betabulls.com</option>
                <option>lweis@gmail.com</option>
                <option>john@gmail.com</option>
            </select>
        </div>
        <div class="form-group col-md-4 col-lg-4 col-sm-3 col-xs-12">
            <label for="inputEmail3" class="control-label">Shared Users :</label>
            <div class="gray-bg divborder" id="divShareToNames"></div>
        </div>
        <div class="form-group col-md-3 col-lg-3 col-sm-4 col-xs-12">
            <label for="inputEmail3" class="control-label">Share Description :</label>
            <div class="">
                <textarea class="form-select form-control" id="txtShareDesc" style="padding: 5px 12px 13px;background-color: #fbfbfb;" placeholder="Description"></textarea>
            </div>

        </div>
        <div class="form-group col-md-2 col-lg-2 col-sm-3 col-xs-12 no-padding-right" style="padding:25px 5px;">
            <label for="inputEmail3" class="control-label" style="float:left;">Can Edit?:</label>
            <div class="">
                <label style="float:left; margin-left:5px; ">
                    <input type="checkbox" value="" id="chkType" class="inverted" name="chkCanEditType">
                    <span class="text"></span>
                </label>
            </div>
            <div class="col-md-12 col-lg-12 col-sm-12 col-xs-12 no-padding-left no-padding-right" style="margin-top:19px;">
                <button class="btn btn-warning btn-md btnSearch pull-right" id="btnCancelShare">Cancel</button>
                <button class="btn btn-primary btn-md btnSearch pull-right" onclick="" id="btnSave" style="margin-right:10px;">Share</button>
            </div>
        </div>
        
    </div>
    <!-- /Share Document -->

    <div class="btn-group pull-right folder-dropdown" style="margin:18px 0px 12px 20px;">
        <a href="javascript:void(0);" onclick="ClearAdd()" class="btn btn-primary" id="btnAddFileorFolder" data-toggle="modal" data-target=".Addfolder">@*<i class="fa fa-folder-open"></i>*@&nbsp;&nbsp;Add File/Folder</a>
    </div>

    <div class="col-lg-12 col-sm-12 col-xs-12 manage-division" style="padding:0px;">
        <input type="hidden" id="hdnParentId" />
        

        <div class="well">
            <div class="table-responsive">
                <table class="table table-striped table-bordered table-hover simpledatatable" role="grid" id="tblDocuments">
                    <thead><tr role='row'><th style='text-align:left;'>ID&nbsp;</th><th style='text-align:left'>Document Name</th><th style='text-align:left; width:110px;'>Type&nbsp;</th><th style='text-align:left; width:150px;'>Who has Access?</th><th style='text-align:left; width:150px'>Uploaded&nbsp;On &nbsp;</th><th style='width:144px;text-align:left;'>Valid&nbsp;From&nbsp;</th><th style='width:144px;text-align:left;'>Valid&nbsp;Upto&nbsp;</th><th style='width:120px;'>Actions</th></tr></thead>
                    <tbody>
                        <tr>
                            <td>
                                2
                            </td>
                            <td style="text-align:left;">
                                <i class="fa fa-file-text-o file-color"></i>
                                &nbsp;Test
                            </td>
                            <td style="text-align:left;">
                                General
                            </td>
                            <td style="text-align:left;">
                                <a href="javascript:void(0);" class="" data-toggle="modal" data-target=".AccessUsers" data-placement="top" data-original-title="List of Users" aria-describedby="tooltip797909">List of Users</a>
                            </td>
                            <td>
                                1-30-2017
                            </td>
                            <td></td>
                            <td></td>
                            <td valign="middle" style="width:144px;">
                                <span data-toggle="modal" data-target=".EditFolder"><a href="javascript:void(0);" class="" data-toggle="tooltip" data-tooltip="tooltip" data-placement="top" data-original-title="Edit"><i class="fa fa-pencil-square-o editpopover"></i></a></span>
                                <a href="#" data-toggle="tooltip" data-placement="top" data-original-title="Delete" id="bootbox-confirm" class="bootbox-confirm"><i class="fa fa-trash-o deletepopover"></i> </a>
                                <a href="#" class="ShareClick" data-toggle="tooltip" data-placement="top" id="ShareClick" data-original-title="Share"><i class="fa fa-share-square-o sharepopover"></i></a>
                            </td>
                        </tr>

                        <tr>
                            <td>
                                1
                            </td>
                            <td style="text-align:left;">
                                <i class="fa fa-file-text-o file-color"></i>
                                Title1
                            </td>
                            <td style="text-align:left;">
                                General
                            </td>
                            <td style="text-align:left;">
                               <span> <a href="javascript:void(0);" class="" data-toggle="modal" data-tooltip="tooltip" data-target=".AccessUsers" data-placement="top" data-original-title="List of Users" aria-describedby="tooltip797909">List of Users</a>
                            </td>
                            <td>
                                1-28-2017
                            </td>
                            <td></td>
                            <td></td>
                            <td valign="middle" style="width:144px;">
                                <span  data-toggle="modal" data-target=".EditFolder"><a href="javascript:void(0);" class=""  data-toggle="tooltip" data-tooltip="tooltip" data-placement="top" data-original-title="Edit"><i class="fa fa-pencil-square-o editpopover"></i></a></span>
                                <a href="#" data-toggle="tooltip" data-placement="top" data-original-title="Delete" id="bootbox-confirm" class="bootbox-confirm"><i class="fa fa-trash-o deletepopover"></i> </a>
                                <a href="#" class="ShareClick" data-toggle="tooltip" data-placement="top" id="ShareClick" data-original-title="Share"><i class="fa fa-share-square-o sharepopover"></i></a>
                            </td>
                        </tr>
                    </tbody>

                </table>

            </div>
        </div>
    </div>
</div>
<!-- /Page Body -->
<!-- Add File/Folder -->
<div class="modal fade Addfolder" id="formAddFolder" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel" aria-hidden="true" style="display: none;">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
                <h4 class="modal-title text-center" id="myLargeModalLabel"><strong>Create Folder/File</strong></h4>
            </div>
            <div class="modal-body">

                <form asp-controller="Document" asp-action="ManageFolders_Files" method="post" enctype="multipart/form-data" class="form-horizontal col-lg-12 col-sm-12 col-xs-12" role="form">
                    <input asp-for="ParentId" type="hidden" id="hdnParent" />
                    <input asp-for="NavigationMenu" type="hidden" id="hdnNavigation" />
                    <div class="form-group">
                        <label for="inputEmail3" class="col-sm-6 col-md-3 col-sm-offset-1 col-lg-3 col-xs-12 col-md-offset-2 col-lg-offset-2 control-label no-padding-right">Select File/Folder :</label>
                        <div class="col-sm-5 col-xs-12">
                            <select asp-for="IsFolder" class="form-select form-control" id="optFile" style="margin-bottom:12px;">
                                <option value=true>Folder</option>
                                <option value=false selected>File</option>
                            </select>
                        </div>
                        <div id="divFileType" hidden>
                            <label for="inputEmail3" class="col-sm-6 col-md-3 col-sm-offset-1 col-lg-3 col-xs-12 col-md-offset-2 col-lg-offset-2 control-label no-padding-right">Document Type :</label>
                            <div class="col-sm-5 col-xs-12">
                                <select asp-for="Category" class="form-select form-control" id="categoryType" style="margin-bottom:8px;">
                                    <option value="General" selected>General</option>
                                    <option value="Policy">Policy</option>
                                </select>
                            </div>
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="inputEmail3" class="col-sm-6 col-md-3 col-sm-offset-1 col-lg-3 col-xs-12 col-md-offset-2 col-lg-offset-2 control-label no-padding-right">Enter Title :<sup style="color:red">*</sup></label>
                        <div class="col-sm-5 col-xs-12">
                            <input asp-for="DocumentTitle" type="text" class="form-select form-control" id="txtName" style="margin-bottom:8px;">
                            <span asp-validation-for="DocumentTitle" class="asterisk" style="margin:0px;"></span>
                        </div>
                    </div>

                    <div class="form-group" hidden>
                        <label for="inputEmail3" class="col-sm-6 col-md-3 col-sm-offset-1 col-lg-3 col-xs-12 col-md-offset-2 col-lg-offset-2 control-label employee-inputcol">Upload File :<sup style="color:red">*</sup></label>
                        <div class="col-sm-5" style="margin-left:15px;">
                            <div class="form-group custom-inputgroup">
                                <input type="file" class="file" id="flpUpload" name="flpUpload">
                                <div class="input-group">
                                    <span class="input-group-addon"><i class="fa fa-file file-color"></i></span>
                                    <input asp-for="Path" type="text" style="cursor:auto !important" class="form-control input-sm" disabled placeholder="Upload File" id="txtUploadPath">
                                    <span class="input-group-btn">
                                        <button class="browse btn btn-primary input-sm" id="btnbrowse" type="button"><i class="fa fa-upload"></i></button>
                                    </span>
                                </div>
                                <span id="MsgUploadError" class="asterisk" style="margin:0px;" hidden>Please select the file.</span>
                            </div>
                        </div>
                    </div>

                    <div class="form-group custom-inputgroup">
                        <label for="inputEmail3" class="col-sm-6 col-md-3 col-sm-offset-1 col-lg-3 col-xs-12 col-md-offset-2 col-lg-offset-2 control-label employee-inputcol">Description :</label>
                        <div class="col-sm-5 col-xs-12">
                            <textarea asp-for="Description" class="form-control" id="txtDescription"></textarea>
                            <span asp-validation-for="Description" class="asterisk" style="margin-left: 0px;"></span>
                        </div>
                    </div>
                    <div class="form-group" hidden id="divValidity">
                        <label for="inputEmail3" class="col-sm-6 col-md-3 col-lg-3 col-xs-12 col-md-offset-2 col-lg-offset-2 control-label employee-inputcol">Validity :</label>
                        <div class="col-sm-6">
                            <div class="checkbox" id="validity-date" style="margin-bottom:15px;">
                                <label style="float:left;">
                                    <input asp-for="Isvalid" type="checkbox" value="validitycheck" id="chkvaliditycheck" class="inverted" name="chkvaliditycheck">
                                    <span class="text"></span>
                                </label>
                            </div>
                            <div class="validitycheck" id="divValidFromAndTo" hidden>
                                <div class="form-group">
                                    <label for="inputEmail3" class="col-sm-6 col-md-3 col-lg-3 control-label no-padding-right" style="padding-left:0px;">Valid From :<sup style="color:red">*</sup></label>
                                    <div class="col-sm-6">
                                        <div class="input-group">
                                            <input asp-for="ValidFrom" class="form-control date-picker" id="id-date-picker-1-txtValidFrom" type="text" style="cursor:default" readonly data-date-format="mm-dd-yyyy">
                                            <span class="input-group-addon">
                                                <i class="fa fa-calendar themeprimary"></i>
                                            </span>
                                        </div>
                                        <span id="MsgValidFromError" class="asterisk" style="margin:0px;" hidden>Valid from date is required.</span>
                                    </div>
                                </div>
                                <div class="form-group">
                                    <label for="inputEmail3" class="col-sm-6 col-md-3 col-lg-3 control-label no-padding-right" style="padding-left:0px;">Valid Upto :<sup style="color:red">*</sup></label>
                                    <div class="col-sm-6">
                                        <div class="input-group">
                                            <input asp-for="ValidUpto" class="form-control date-picker" id="id-date-picker-1-txtValidUpto" type="text" style="cursor:default" readonly data-date-format="mm-dd-yyyy">
                                            <span class="input-group-addon">
                                                <i class="fa fa-calendar themeprimary"></i>
                                            </span>
                                        </div>
                                        <span id="MsgValidUptoError" class="asterisk" style="margin:0px;" hidden>Valid upto date is required.</span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <hr class="folders-hr" />
                    <div class="col-md-12 col-lg-12 col-sm-12 col-xs-12 text-center center-block">
                        <a href="/Document/ManageFolders_Files">
                            <button type="submit" class="btn btn-primary btn-md btnSearch" id="btnSubmit">Create</button></a>
                            @*<a href="#" class="btn btn-primary btn-md btnSearch" id="btnSubmit">Create</a>*@
                            <a href="/Document/ManageFolders_Files" class="btn btn-warning btn-md btnSearch" data-dismiss="modal" id="btnCancelAdd">Cancel</a>
</div>
                </form>

            </div>
        </div><!-- /.modal-content -->
    </div><!-- /.modal-dialog -->
</div>
<!-- /Add File/Folder -->
<!--Edit File/Folder-->
<div class="modal fade EditFolder" id="EditFolder" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel" aria-hidden="true" style="display: none;">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
                <h4 class="modal-title text-center" id="myLargeModalLabel"><strong>Edit Folder/Document</strong></h4>
                <input type="hidden" id="hdnDocumentId" />
            </div>
            <div class="modal-body">
                <form class="form-horizontal col-lg-12 col-sm-12 col-xs-12" role="form">
                    <div class="form-group">
                        <label for="inputEmail3" class="col-sm-5 col-md-2 col-sm-offset-1 col-lg-2 col-xs-12 col-md-offset-3 col-lg-offset-3 control-label no-padding-right">Title :</label>
                        <div class="col-sm-4">
                            <input type="text" class="form-select form-control" id="txtFolderName" style="margin-bottom:8px;" placeholder="Folder/File Name">
                            <span id="MsgEditTitle" class="asterisk" style="margin:0px;" hidden>Please enter title.</span>
                        </div>

                    </div>
                    <div class="form-group" id="divEditFileType" hidden>
                        <label for="inputEmail3" class="col-sm-5 col-md-2 col-sm-offset-1 col-lg-2 col-xs-12 col-md-offset-3 col-lg-offset-3 control-label employee-inputcol">Document Type :</label>
                        <div class="col-sm-4">
                            <select class="form-select form-control" id="categoryTypeEdit" style="margin-bottom:8px;">
                                <option value="General">General</option>
                                <option value="Policy">Policy</option>
                            </select>
                        </div>
                    </div>
                    <div class="form-group">
                        <label for="inputEmail3" class="col-sm-5 col-md-2 col-sm-offset-1 col-lg-2 col-xs-12 col-md-offset-3 col-lg-offset-3 control-label employee-inputcol">Description :</label>
                        <div class="col-sm-4">
                            <textarea class="form-control" id="txtDesEdit"></textarea>
                            <span id="MsgEditDesc" class="asterisk" style="margin:0px;" hidden>Description should be less than 500 characters.</span>
                        </div>
                    </div>

                    <div class="form-group" id="divValidityEdit">
                        <label for="inputEmail3" class="col-sm-6 col-md-2 col-lg-2 col-xs-12 col-md-offset-3 col-lg-offset-3 control-label employee-inputcol">Validity :</label>
                        <div class="col-sm-6">
                            <div class="checkbox" id="validity-dateEdit" style="margin-bottom:15px;">
                                <label style="float:left;">
                                    <input type="checkbox" class="inverted" value="validitycheck" id="chkvaliditycheckEdit" name="chkvaliditycheckEdit" checked>
                                    <span class="text"></span>
                                </label>
                            </div>
                            <div class="validitycheck" id="divEditValidFromAndTo">
                                <div class="form-group">
                                    <label for="inputEmail3" class="col-sm-6 col-md-3 col-lg-3 control-label no-padding-right" style="padding-left:0px;">Valid From :</label>
                                    <div class="col-sm-6">
                                        <div class="input-group">
                                            <input class="form-control date-picker" id="id-date-picker-1-txtValidFromEdit" type="text" style="cursor:default" readonly data-date-format="mm-dd-yyyy">
                                            <span class="input-group-addon">
                                                <i class="fa fa-calendar themeprimary"></i>
                                            </span>
                                        </div>
                                        
                                    </div>
                                </div>
                                <div class="form-group">
                                    <label for="inputEmail3" class="col-sm-6 col-md-3 col-lg-3 control-label no-padding-right" style="padding-left:0px;">Valid Upto :</label>
                                    <div class="col-sm-6">
                                        <div class="input-group">
                                            <input class="form-control date-picker" id="id-date-picker-1-txtValidUptoEdit" type="text" style="cursor:default" readonly data-date-format="mm-dd-yyyy">
                                            <span class="input-group-addon">
                                                <i class="fa fa-calendar themeprimary"></i>
                                            </span>
                                        </div>
                                        
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </form>
                <hr class="location-hr" />
                <div class="col-md-12 col-lg-12 col-sm-12 col-xs-12 text-center center-block">
                    <a href="/Document/ManageFolders_Files"> <button class="btn btn-primary btn-md btnSearch" id="btnSave">Save</button></a>
                    <a href="/Document/ManageFolders_Files"> <button class="btn btn-warning btn-md btnSearch" data-dismiss="modal" id="btnCancel">Cancel</button></a>
</div>
            </div>
        </div><!-- /.modal-content -->
    </div><!-- /.modal-dialog -->
</div>
<!--/Edit File/Folder-->
<!--List of users-->
<div class="modal fade AccessUsers" id="accessUsersList" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel" aria-hidden="true" style="display: none;">
    <div class="modal-dialog modal-md">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
                <h4 class="modal-title text-center" id="myLargeModalLabel"><strong>Access List for [File/Folder]</strong></h4>
            </div>
            <div class="modal-body">
                <form class="form-horizontal col-lg-12 col-sm-12 col-xs-12" role="form">

                    <table class="table table-striped table-bordered table-hover dataTable no-footer" role="grid" aria-describedby="simpledatatable_info" id="tblAccessUsers"><thead><tr role="row"><th class="text-center">User</th><th class="text-center">Shared On</th><th class="text-center">Can Edit?</th></tr></thead><tbody><tr><td style="text-align:left"> raju@betabulls.com</td><td class="text-center">01-17-2017</td><td class="text-center">Yes</td></tr><tr><td style="text-align:left"> junaid.md@betabulls.com</td><td class="text-center">01-17-2017</td><td class="text-center">Yes</td></tr><tr><td style="text-align:left"> gnreddy64@gmail.com</td><td class="text-center">01-17-2017</td><td class="text-center">Yes</td></tr></tbody></table>

                </form>
                <hr class="fileaccessed-hr" />
                <div class="col-md-12 col-lg-12 col-sm-12 col-xs-12 text-center center-block">
                    <a href="#" class="btn btn-primary btn-md btnSearch" data-dismiss="modal" id="btnCancelShare">OK</a>
                </div>
            </div>
        </div><!-- /.modal-content -->
    </div><!-- /.modal-dialog -->
</div>
<!--/List of users-->
<!--Delete modal-->
<div class="modal fade" id="formConfirm" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-md">
        <div class="modal-content">
            <div class="modal-header" style="background:transparent;">
                <button type="button" class="close" data-dismiss="modal"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
                <h5 class="modal-title" id="frm_title">Delete Confirmation</h5>
            </div>
            <div class="modal-body delete-text-align" id="frm_body">Are you sure to delete this record?</div>
            <div class="modal-footer" style="background:transparent;">
                <button type="button" class="btn btn-warning col-sm-2 pull-right" data-dismiss="modal" id="frm_cancel">No</button>
                <a href="#"><button style='margin-left:10px; margin-right:15px;' type="button" class="btn btn-primary col-sm-2 pull-right" id="frm_submit">Yes</button></a>

            </div>
        </div>
    </div>
</div>
<!--/Delete modal-->

<environment>
    <script src="~/assets/js/bootbox/bootbox.js"></script>
    <link href="~/assets/css/upload_policy.css" rel="stylesheet" />
    <link href="~/assets/css/dataTables.bootstrap.css" rel="stylesheet" />

    <style>
        #tblDocuments > tbody > tr > td, .table > tbody > tr > th, .table > tfoot > tr > td, .table > tfoot > tr > th, .table > thead > tr > td, .table > thead > tr > th {
            text-align: left !important;
        }

        .table > tbody > tr > td, .table > tbody > tr > th, .table > tfoot > tr > td, .table > tfoot > tr > th, .table > thead > tr > td, .table > thead > tr > th {
            vertical-align: middle;
        }


        .table > thead > tr > th {
            background-color: #c1c1c1;
        }

        .form-horizontal .form-group {
            margin-bottom: 8px;
        }

        .checkbox {
            text-align: center;
        }


        .form-horizontal .control-label {
            text-align: left;
        }

        .checkbox, .radio {
            margin-top: 5px;
            margin-bottom: 5px;
        }

        .btn-primary.active.focus, .btn-primary.active:focus, .btn-primary.active:hover, .btn-primary:active.focus, .btn-primary:active:focus, .btn-primary:active:hover, .open > .dropdown-toggle.btn-primary.focus, .open > .dropdown-toggle.btn-primary:focus, .open > .dropdown-toggle.btn-primary:hover {
            border-color: transparent;
        }

        .modal-body {
            overflow: hidden;
        }

        .ajax-upload-dragdrop {
            padding-top: 0px;
        }

        .file {
            visibility: hidden;
            position: absolute;
        }

        .asterisk {
            color: #ff0000;
            margin-top: -29px;
            margin-left: 292px;
        }

        .float-left {
            width: 87%;
            margin-bottom: 8px;
            float: left;
        }

        .custom-inputgroup .input-group {
            width: 92%;
        }

        .DTTT.btn-group {
            position: absolute !important;
            display: none !important;
        }
    </style>

</environment>

<environment>

    <script src="~/assets/js/datatable/jquery.dataTables.min.js"></script>
    <script src="~/assets/js/datatable/ZeroClipboard.js"></script>
    <script src="~/assets/js/datatable/dataTables.tableTools.min.js"></script>
    <script src="~/assets/js/datatable/dataTables.bootstrap.min.js"></script>
    <script src="~/assets/js/datatable/datatables-init.js"></script>
    <script src="~/assets/js/bootbox/bootbox.js"></script>

    <script src="~/assets/js/select2/select2.js"></script>
    <script src="~/assets/js/toastr/toastr.js"></script>
    <script>
        $(document).ready(function (){ 
            
           
            //$('#popup').click(function () {
            //    $('#ShareFiles').slideToggle("slow");

            //});

            $('.ShareClick').click(function (e) {

            if ($(e.target).closest('.ShareClick, #popup').length) {
                $('#popup').slideToggle("slow");
            } 
            //else if ($('#popup').is(':visible')) {
            //    $('#popup').hide();
            //}
            
        });
       
        $('#btnCancelShare').click(function () {
            $('#popup').slideToggle("slow");
         
        });
        });
        var value = "chkvaliditycheckEdit";
       
    </script>
    <script>
 $(function () {
  $('[data-toggle="tooltip"]').tooltip()
})
    </script>
    @*<script>
 $(function () {
     $("[data-toggle=tooltip]").tooltip();
     $(".upload").hide();
     $('input[type="radio"]').click(function () {
         if ($(this).attr("value") == "createnew") {
             $(".box").not(".createnew").hide();
             $(".createnew").show();
         }
         if ($(this).attr("value") == "upload") {
             $(".box").not(".upload").hide();
             $(".upload").show();
         }
     });
    </script>*@

    @*<script>

        var ClientID = '@ViewBag.MasterClientID';
        var oTable1 = $('#tblDocuments').dataTable({
            "sDom": "Tflt<'row DTTTFooter'<'col-sm-6'i><'col-sm-6'p>>",
            "aLengthMenu": [
               [5, 10, 20, 50, 100, -1],
               [5, 10, 20, 50, 100, "All"]
            ],
            "iDisplayLength": 10,
            "oTableTools": {
                "aButtons": [
                    "copy", "csv", "xls", "pdf", "print"
                ],
                // "sSwfPath": "http://localhost:54505/assets/swf/copy_csv_xls_pdf.swf"//"assets/swf/copy_csv_xls_pdf.swf"
            },
            "language": {
                "search": "",
                "sLengthMenu": "_MENU_",
                "oPaginate": {
                    "sPrevious": "Prev",
                    "sNext": "Next"
                }
            },
            "aoColumns": [null, null, null, null, { "bSortable": false }, { "bSortable": false }, { "bSortable": false }],
            "aaSorting": []
        });
    </script>*@
    
    @*<script>

        var navBool = true; //Not to overwrite the folder when selected

        var oTable1 = $('#tblDocuments').dataTable({

            "sDom": "Tflt<'row DTTTFooter'<'col-sm-6'i><'col-sm-6'p>>",
            "aLengthMenu": [
               [5, 10, 20, 50, 100, -1],
               [5, 10, 20, 50, 100, "All"]
            ],
            "iDisplayLength": 10,
            "oTableTools": {
                "aButtons": [
                    "copy", "csv", "xls", "pdf", "print"
                ],
                "sSwfPath": "http://localhost:54505/assets/swf/copy_csv_xls_pdf.swf"//"assets/swf/copy_csv_xls_pdf.swf"
            },
            "language": {
                "search": "",
                "sLengthMenu": "_MENU_",
                "oPaginate": {
                    "sPrevious": "Prev",
                    "sNext": "Next"
                }
            },
            "aoColumns": [null, null, null, { "bSortable": false }, null, null, null, { "bSortable": false }],
            "aaSorting": []
        });

        $('#btnbrowse').on('click', function () {

            var file = $(this).parent().parent().parent().find('.file');
            file.trigger('click');
        });

        $(document).on('change', '.file', function () {

            $(this).parent().find('.form-control').val($(this).val().replace(/C:\\fakepath\\/i, ''));
        });

    </script>
    <script>

        var parentID = $('#hdnParentId').val();
        var navTitle = "";

        function Sharedocument() {

            var shareTolist = new Array();
            var errorShareTolist = new Array();
            var errorMsg = '';

            var btn = $('#btnShare');
            btn.button('loading');

            var documentId = $('#hdnDocumentId').val();
            var ClientID = $('#ddlMasterClient :selected').val();

            if ($("#txtShareTo :selected").text() == '') {
                Notify("Please select user name", 'top-right', '5000', 'warning', 'fa-warning', true);
                btn.button("reset");
                return false;
            }

            var canEdit = $('#chkType').is(':checked');
            var emailReg = /^([\w-\.]+@@([\w-]+\.)+[\w-]{2,4})?$/;
            var ids = "";

            $("#txtShareTo :selected").each(function (i) {

                var shareDescription = $('#txtShareDesc').val();
                var shareTo = $(this).text();
                var shareFrom = null;
                var loginDetails = GetLogin();

                if (loginDetails != null && loginDetails != '')

                    shareFrom = loginDetails.split(',')[0];
                var data1 = {

                    "DocumentID": documentId,
                    "ShareFrom": shareFrom,
                    "ShareTo": shareTo,
                    "CanEdit": canEdit,
                    "Description": shareDescription,
                    "ParentId": $("#hdnParentId").val()
                }
                $.ajax({

                    url: '/Document/ShareDocument',
                    type: 'Post',
                    async: false,
                    data: { documentShare: data1, clientID: ClientID },
                    success: function (data) { 
                        var dataJson = data;
                        if (dataJson == "Document has been Shared successfully") {
                            ids = dataJson;
                        }
                        else {
                            ids = dataJson;
                        }
                        if (ids == "Document has been Shared successfully") {

                            shareTolist.push(shareTo + '\n');
                        }
                        else {

                            errorMsg = ids;
                            errorShareTolist.push(shareTo + '\n');
                        }

                        AccessDocument123(documentId);
                        $("#txtShareDesc").val('');
                        $('#shareFolder').hide();
                    }
                })
            });

            var documentTitle = $("#lblDocumentTitle").text();

            if (shareTolist.length > 0) {

                SuccessMessage("Document '" + documentTitle + "' have been shared to '" + shareTolist + "' successfully.");
                //NotifyMessage("Document '" + documentTitle + "' have been shared to '" + shareTolist + "' successfully.", 'top-right', '5000', 'success', 'fa-check', true);
                $('#chkType').attr('checked', false);
                btn.button("reset");

                $('#txtShareTo :selected').each(function () {
                    $(this).prop('selected', false);
                    $('.select2-selection__rendered').empty();
                })
            }
            if (errorShareTolist.length > 0) {
                if (errorMsg == "Email does not exist in the user table.") {
                    NotifyMessage("Email does not exist in the user table.", 'top-right', '5000', 'warning', 'fa-warning', true);
                    btn.button("reset");
                }
                else {
                    NotifyMessage("Share to email already exists with '" + errorShareTolist + "' please select a different user to share", 'top-right', '5000', 'warning', 'fa-warning', true);
                    btn.button("reset");
                }
                $('#txtShareTo :selected').each(function () {
                    $(this).prop('selected', false);
                    $('.select2-selection__rendered').empty();
                })
            }
        }

        function EditDocument(documentId, isFolder) {

            $('#MsgEditTitle').hide();
            $('#MsgEditDesc').hide();
            $('#MsgEditValidFrom').hide();
            $('#MsgEditValidUpto').hide();

            $('.select2-selection__rendered').empty();
            $('.modal-content').show();
            if ($('#OpenFolderTable').is(':visible') == true)
                $('#OpenFolderTable').show();
            else
                $('#OpenFolderTable').hide();

            $('#ShareFiles').hide();
            $('#chkType').attr('checked', false);

            var ClientID = $('#ddlMasterClient :selected').val();
            $('.modal-body').find('.form-group').eq(2).val('').show();
            $('.modal-body').find('.form-group').eq(5).val('').show();

            if (isFolder) {
                $('#divEditFileType').hide();
                $("#divValidityEdit").hide();
            }
            else {
                $('#divEditFileType').show();
                $("#divValidityEdit").show();
            }
            $('.page-sidebar').css('z-index', '2');
            $('.navbar').css('z-index', '200');
            $.ajax({
                url: '/Document/GetDocumentById',
                data: { documentId: documentId, clientID: ClientID },
                datatype: 'json',
                contentType: 'application/json',
                async: false,
                processdata: true,

                success: function (data) {

                    var documentId = JSON.parse(data).DocumentID;
                    var documentTitle = JSON.parse(data).DocumentTitle;
                    var documentDesc = JSON.parse(data).Description;
                    var documentCategoryType = JSON.parse(data).Category;
                    var credate = new Date(JSON.parse(data).CreatedDate);

                    var createdMonth = (credate.getMonth() + 1);
                    var createdMonthFinal = (createdMonth < 9 ? '0' + createdMonth : createdMonth);
                    var createdDate = credate.getDate();
                    var creatededDateFinal = (createdDate < 10 ? '0' + createdDate : createdDate);
                    var createddate = (createdMonthFinal + '-' + creatededDateFinal + '-' + credate.getFullYear());
                    var createdBy = JSON.parse(data).CreatedBy;
                    var modifydate = new Date(JSON.parse(data).ModifiedDate);
                    var ModifiedMonth = (modifydate.getMonth() + 1);

                    if (ModifiedMonth != '')
                        var ModifiedMonthFinal = (ModifiedMonth < 9 ? '0' + ModifiedMonth : ModifiedMonth);
                    var ModifiedDate = (modifydate.getDate());
                    if (ModifiedDate != '')
                        var ModifiedDateFinal = (ModifiedDate < 10 ? '0' + ModifiedDate : ModifiedDate);

                    var modifieddate = (ModifiedMonthFinal + '-' + ModifiedDateFinal + '-' + modifydate.getFullYear());
                    var modifiedDate = JSON.parse(data).ModifiedDate == null ? '' : modifieddate;
                    var modifiedBy = JSON.parse(data).ModifiedBy;

                    $('#hdnDocumentId').val(documentId);
                    $('#txtFolderName').val(documentTitle);
                    $('#txtDesEdit').val(documentDesc);
                    $('#lblCreatedDate').text(createddate);
                    $('#lblCreatedBy').text(createdBy);
                    $('#lblModifiedDate').text(modifiedDate);
                    $('#lblModifieBy').text(modifiedBy);

                    if (documentCategoryType != null)
                        $('#categoryTypeEdit').val(documentCategoryType);
                    else
                        $('#categoryTypeEdit').val('Select');

                    if (JSON.parse(data).ValidFrom != null && JSON.parse(data).ValidFrom != "") {

                        var ediefromdate = new Date(JSON.parse(data).ValidFrom);
                        var edieTodate = new Date(JSON.parse(data).ValidUpto);

                        $('#id-date-picker-1-txtValidFromEdit').val((ediefromdate.getMonth() + 1) + '-' + ediefromdate.getDate() + '-' + ediefromdate.getFullYear());
                        $('#id-date-picker-1-txtValidUptoEdit').val((edieTodate.getMonth() + 1) + '-' + edieTodate.getDate() + '-' + edieTodate.getFullYear());
                        $("#chkvaliditycheckEdit").prop('checked', true).change();
                    }
                }
            })
        }

        function SharePopup(id, documentTitle) {

            if ($('#OpenFolderTable').is(':visible') == true)
                $('#OpenFolderTable').show();
            else
                $('#OpenFolderTable').hide();

            $("#lblDocumentTitle").text(documentTitle);
            $('#hdnDocumentId').val(id);

            AccessDocument123(id);

            $("#txtShareTo :selected").each(function () {
                $(this).prop("selected", false).change();
            });

            $("#txtShareDesc").val('');
            $("#ShareFiles").show();
        }

        var docID = new Array();
        var i = 0;
        var nav = '';
        function OpenFolder(documentID, documentTitle) {

            if (navBool == false)
                return false;

            if (navBool == true)
                navBool = false;

            docID.push(documentID);
            $("#hdnParentId").val(documentID);
            $("#ShareFiles").hide();

            OpenFolderDocuments(documentID, '/Document/GetFilesByParentID');

            navTitle += documentTitle + " / ";
            var title = navTitle.split('/');
            $('#hdnNavigation').html('');

            for (var s = 0; s <= title.length - 2; s++) {
                $('#hdnNavigation').append((docID[s] + ',' + title[s]) + '/');
            }

            var Folder = 'class="fa fa-folder-open folder-color"';

            $("#navigationmenu").append('<a href=# style="vertical-align:-webkit-baseline-middle;  margin:0px 0px 0px 5px;" onclick=Navigate(' + (i++) + ',' + documentID + ') ><i ' + Folder + ' ></i>' + documentTitle + ' /</a>').show(); //+ '<span style="vertical-align:-webkit-baseline-middle;">/</span>'
            $("#OpenFolder").toggle();
        }

        function Navigate(count, documentID) {

            if (navBool == false)
                return false;
            if (navBool == true)
                navBool = false;

            $("#navigationmenu").empty();
            nav = '';

            var title = navTitle.split('/');

            for (var s = 0; s <= count; s++) {

                nav += title[s] + '/';
                var Folder = 'class="fa fa-folder-open folder-color"';
                $("#navigationmenu").append('<a href=# style="vertical-align:-webkit-baseline-middle;  margin:0px 0px 0px 5px;" onclick=Navigate(' + s + ',' + docID[s] + ') ><i ' + Folder + ' ></i>' + title[s] + '/</a>').show()
            }

            navTitle = nav;
            i = count + 1;

            for (var f = i; f < docID.length; f++)
                docID.splice(f, docID.length);

            OpenFolderDocuments(documentID, '/Document/GetFilesByParentID');
            $("#hdnParentId").val(documentID);
        }

        function UpdatedocumentDetails(url) {

            $('.select2-selection__rendered').empty();
            if ($("#txtFolderName").val() == undefined || $("#txtFolderName").val() == "") {

                $('#MsgEditTitle').show();
                // Notify('Please enter title name', 'top-right', '5000', 'warning', 'fa-warning', true);
                $("#txtName").focus();
                return false;
            }
            else {
                $('#MsgEditTitle').hide();
            }

            if ($('#OpenFolderTable').is(':visible') == true)
                $('#OpenFolderTable').show();
            else
                $('#OpenFolderTable').hide();

            var ClientID = $('#ddlMasterClient :selected').val();

            if ($("#chkvaliditycheckEdit").is(':checked')) {

                var dateFrom = $('#id-date-picker-1-txtValidFromEdit').val();
                var dateTo = $('#id-date-picker-1-txtValidUptoEdit').val();

                if (dateFrom == "" && dateTo == "") {

                    $('#MsgEditValidFrom').show();
                    $('#MsgEditValidUpto').show();
                    //Notify('Valid From and Valid Upto dates are required.', 'top-right', '5000', 'warning', 'fa-warning', true);
                    btn.button('reset')
                    return false;
                }
                else {

                    $('#MsgEditValidFrom').hide();
                    $('#MsgEditValidUpto').hide();
                }

                if (dateFrom == null || dateFrom == "") {
                    $('#MsgEditValidFrom').show();
                    //Notify('Please select ValidFrom date.', 'top-right', '5000', 'warning', 'fa-warning', true);
                    btn.button('reset')
                    return false;
                }
                else {
                    $('#MsgEditValidFrom').hide();
                }

                if (dateTo == null || dateTo == "") {

                    $('#MsgEditValidUpto').show();
                    //Notify('Please select ValidUpto date', 'top-right', '5000', 'warning', 'fa-warning', true);
                    btn.button('reset')
                    return false;
                }
                else {
                    $('#MsgEditValidUpto').hide();
                }

                if ($("#txtFolderName").val() == undefined || $("#txtFolderName").val() == "") {
                    $('#MsgEditTitle').show();
                    // Notify('Please enter title name', 'top-right', '5000', 'warning', 'fa-warning', true);
                    $("#txtName").focus();
                    btn.button('reset')
                    return false;
                }

                if (Date.parse(dateFrom) > Date.parse(dateTo)) {
                    $('#MsgEditValidUpto').show();
                    $('#MsgEditValidUpto').text('Valid Upto date should not be less than Valid From date.');
                    $('#id-date-picker-1-txtValidUptoEdit').val('');
                    //Notify('Valid Upto date should not be less than Valid From date.', 'top-right', '5000', 'warning', 'fa-warning', true);
                    btn.button('reset')
                    return false;
                }
                else {
                    $('#MsgEditValidUpto').hide();
                }
            }

            var btn = $("#btnSave");
            btn.button('loading');
            var loginDetails = GetLogin();
            var email = loginDetails.split(',')[0];
            var user = loginDetails.split(',')[1];

            var getData = {

                "DocumentTitle": $('#txtFolderName').val(),
                "Description": $('#txtDesEdit').val() == "" ? null : $('#txtDesEdit').val(),
                "Category": $('#categoryTypeEdit').find(':selected').val(),
                "ModifiedBy": user,
                "Isvalid": $("#chkvaliditycheckEdit").is(':checked') == 1 ? true : false,
                "ValidFrom": $("#id-date-picker-1-txtValidFromEdit").val(),
                "ValidUpto": $("#id-date-picker-1-txtValidUptoEdit").val(),
                "ParentId": $('#hdnParentId').val()
            }

            $.ajax({
                type: 'Put',
                url: url,
                data: { documentId: $('#hdnDocumentId').val(), document: getData, clientID: ClientID },
                success: function (data) {

                    if (JSON.parse(data) == "Document has been updated successfully.") {
                        $('.modal-backdrop').css({ position: 'initial' });
                        $('.EditFolder').hide();

                        SuccessMessage("Document '" + $('#txtFolderName').val() + "' has been updated successfully.");
                        //Notify("Document " + '"' + $('#txtFolderName').val() + '"' + " has been updated successfully.", 'top-right', '5000', 'success', 'fa-check', true);

                        OpenFolderDocuments($('#hdnParentId').val(), '/Document/GetFilesByParentID');
                    }
                    else {

                        if (JSON.parse(data) == "The Description should be within 500 characters") {
                            $('#MsgEditDesc').show();
                            return false;
                        }
                        else {
                            $('#MsgEditDesc').hide();
                        }
                        Notify(data, 'top-right', '5000', 'warning', 'fa-warning', true);
                    }
                },
                complete: function () {
                    btn.button('reset');
                }
            })
        }

        function DeleteDocument(documentId, IsFolder, documentTitle) {

            $('#ShareFiles').slideUp();
            $('#chkType').attr('checked', false);
            $('.select2-selection__rendered').empty();

            if ($('#OpenFolderTable').is(':visible') == true)
                $('#OpenFolderTable').show();
            else
                $('#OpenFolderTable').hide();

            $('.page-sidebar').css('z-index', '2');
            $('.navbar').css('z-index', '200');

            var ClientID = $('#ddlMasterClient :selected').val();
            //bootbox.confirm("Are you sure you want to delete?", function (result) {
            bootbox.confirm({
                title: "Delete",
                message: "Are you sure you want to delete?",
                buttons: {
                    cancel: {
                        label: '<i class="fa fa-times"></i> Cancel'
                    },
                    confirm: {
                        label: '<i class="fa fa-check"></i> Confirm'
                    }
                }, callback: function (result) {
                    if (result) {
                        $.ajax({
                            type: 'DELETE',
                            url: '/Document/DeleteDocument',
                            data: { documentId: documentId, clientID: ClientID },
                            dataType: 'json',
                            success: function (data, statusText, resObject) {

                                $('.modal-backdrop').css({ position: 'initial' });
                                $('#formConfirm').hide();
                                if (data == "This document already deleted.") {
                                    if (IsFolder == true)
                                        SuccessMessage("Document '" + documentTitle + "' has been deleted successfully."); //Notify("Document " + '"' + documentTitle + '"' + " has been deleted successfully.", 'top-right', '5000', 'success', 'fa-check', true);
                                    if (IsFolder == false)
                                        SuccessMessage("The folder '" + documentTitle + "' has been deleted successfully."); //Notify("The folder " + '"' + documentTitle + '"' + " has been deleted successfully", 'top-right', '5000', 'success', 'fa-check', true);
                                    GetParentIDByDocumentID(documentId);
                                }
                                else {

                                    Notify(data, 'top-right', '5000', 'warning', 'fa-warning', true);
                                }
                            },
                            complete: function () {

                            },
                            error: function (jqXHR, exception) {
                                var msg = '';
                                if (jqXHR.status === 0) {
                                    msg = 'Not connect.\n Verify Network.';
                                } else if (jqXHR.status == 404) {
                                    msg = 'Requested page not found. [404]';
                                } else if (jqXHR.status == 500) {
                                    msg = 'Internal Server Error [500].';
                                } else if (exception === 'parsererror') {
                                    msg = 'Requested JSON parse failed.';
                                } else if (exception === 'timeout') {
                                    msg = 'Time out error.';
                                } else if (exception === 'abort') {
                                    msg = 'Ajax request aborted.';
                                } else {
                                    msg = 'Uncaught Error.\n' + jqXHR.responseText;
                                }
                                Notify(msg, 'top-right', '5000', 'warning', 'fa-warning', true);
                            }
                        });
                    }
                }
            });
        }

        //$("#txtShareTo").select2({
        //    placeholder: "Select user",
        //    allowClear: true
        //});

        function AccessDocument(Id) {
            debugger;
            $('.modal-content').show();
            var appendData = '';
            var ClientID = $('#ddlMasterClient :selected').val();
            $('#tblAccessUsers').empty();

            $.ajax({
                url: '/Document/GetDocumentsShareByDocumentId',
                type: 'Post',
                data: { documentId: Id, clientID: ClientID },
                success: function (data) {
                    debugger;
                    var data1 = data;//JSON.parse(data);
                    var appendData = "";                  

                    if (data1 != '' && data1 != "Content not found.") {

                        appendData += "<thead><tr role='row'><th class='text-center'>User</th><th class='text-center'>Shared On</th><th class='text-center'>Can Edit?</th></tr></thead><tbody>";
                        $.each(data1, function (i, item) {
                            var SharedOn = new Date(item.shareDate.split('T')[0]);
                            var SharedMonth = (SharedOn.getMonth() + 1);

                            if (SharedMonth != '')
                                var SharedMonthFinal = (SharedMonth < 9 ? '0' + SharedMonth : SharedMonth);
                            var SharedDate = (SharedOn.getDate());

                            if (SharedDate != '')
                                var SharedDateFinal = (SharedDate < 10 ? '0' + SharedDate : SharedDate);

                            var SharedOnFinal = (SharedMonthFinal) + '-' + SharedDateFinal + '-' + SharedOn.getFullYear();
                            appendData += "<tr><td style='text-align:left'> " + item.shareTo + "</td><td  class='text-center' >" + SharedOnFinal + "</td><td  class='text-center'>" + (item.canEdit == false ? 'No' : 'Yes') + "</td>";
                        });
                        appendData += "</tbody>";
                        $('#tblAccessUsers').html(appendData).show();
                    }
                    else {
                       
                        appendData = "<div style='text-align:center'><b>No content available</b></div>";
                        $('#tblAccessUsers').css('border', 'none');
                        $('#tblAccessUsers').html(appendData).show();
                    }
                }
            });
        }

        function AccessDocument123(Id) {
            debugger;
            var appendData = '';
            var ClientID = $('#ddlMasterClient :selected').val();
            $('#divShareToNames').html('').show();
            var Delete = '';

            $.ajax({
                url: '/Document/GetDocumentsShareByDocumentId',
                type: 'Post',
                data: { documentId: Id, clientID: ClientID },
                success: function (data) {
                    debugger;
                    var data1 = data;//JSON.parse(data);
                    if (data1 != '' && data1 != 'Content not found.') {

                        for (var i = data1.length - 1; i >= 0; i--) {
                            Delete = "<a href='#'  class='' onclick=DeleteShare(" + data1[i].documentShareID + ",\"" + data1[i].shareTo + "\")><i class='fa fa-trash-o deletepopover'></i> </a>";
                            appendData += data1[i].shareTo + " " + Delete + "<br />";
                        }
                        $('#divShareToNames').html('<p>' + appendData + '</p>').show();
                    }
                }
            })
        }

       
                    
              
        

        function OpenFolderDocuments(documentID, url) {
            {
                var ClientID = $('#ddlMasterClient :selected').val();

                $.ajax({
                    url: url,
                    type: 'GET',
                    data: { parentID: documentID, clientID: ClientID },

                    success: function (data) {
                        oTable1.fnClearTable();
                        var data1 = JSON.parse(data);
                        var count;
                        if (data1 != null && data1 != "Content not found") {
                            $.each(data1, function (i, item) {

                                var Folder = 'class="fa fa-folder-open folder-color"';
                                var File = 'class="fa fa-file-text-o file-color"';
                                var image = item.IsFolder == true ? Folder : File;

                                var FolderLink = "<a href='#' onclick='OpenFolder(" + item.DocumentID + ",\"" + item.DocumentTitle + "\")'><div style='float:left;'><i " + image + " ></i></div><div style='margin-left:16px;'> " + item.DocumentTitle + "</div></a>";
                                var FileLink = "<a href='#' onclick='Downloadfile(" + item.DocumentID + ")'><div style='float:left;'><i " + image + " ></i></div><div style='margin-left:16px;'> " + item.DocumentTitle + "</div></a>";
                                var Link = item.IsFolder == true ? FolderLink : FileLink;

                                var credate = new Date(item.CreatedDate);
                                var uploadmonth = (credate.getMonth() + 1);
                                if (uploadmonth != '')
                                    var finalmonth = (uploadmonth < 9 ? '0' + uploadmonth : uploadmonth);
                                var uploaddate = credate.getDate() - 1;
                                if (uploaddate != '')
                                    var finalDate = (uploaddate < 10 ? '0' + uploaddate : uploaddate);
                                var createdDate = (finalmonth) + '-' + finalDate + '-' + credate.getFullYear();


                                var vldFrom = new Date(item.ValidFrom);  //item.ValidFrom
                                var frmmonth = (vldFrom.getMonth() + 1);
                                if (frmmonth != '')
                                    var FinalfrmMonth = (frmmonth < 9 ? '0' + frmmonth : frmmonth);
                                var frmdate = vldFrom.getDate();
                                if (frmdate != '')
                                    var FinalfrmDate = (frmdate < 10 ? '0' + frmdate : frmdate);
                                var validFrom = (FinalfrmMonth + '-' + FinalfrmDate + '-' + vldFrom.getFullYear());


                                var vldUpto = new Date(item.ValidUpto);  //item.ValidFrom
                                var tomonth = (vldUpto.getMonth() + 1);
                                if (tomonth != '')
                                    var FinaltoMonth = (tomonth < 9 ? '0' + tomonth : tomonth);
                                var todate = vldUpto.getDate();
                                if (todate != '')
                                    var FinaltoDate = (todate < 10 ? '0' + todate : todate);
                                var validTo = (FinaltoMonth + '-' + FinaltoDate + '-' + vldUpto.getFullYear());

                                var WhoHasAccess = "<a href='javascript:void(0);' onclick='AccessDocument(" + item.DocumentID + ")' class='' data-toggle='modal' data-target='.AccessUsers'  data-placement='top' >List of Users</a>";   //AccessUsers  data-original-title='List of Shared Users'

                                var Edit = "<a href='javascript:void(0);' onclick='EditDocument(" + item.DocumentID + ',' + item.IsFolder + ")' class='' data-toggle='modal' data-target='.EditFolder'  data-placement='top' data-original-title='Edit' ><i class='fa fa-pencil-square-o editpopover'></i></a>";
                                var Share = "<a href='#'  class='' onclick='SharePopup(" + item.DocumentID + ",\"" + item.DocumentTitle + "\")' data-placement='top' data-original-title='Share'><i class='fa fa-share-square-o sharepopover'></i></a>";
                                var Delete = "<a href='#' onclick='DeleteDocument(" + item.DocumentID + ',' + item.IsFolder + ",\"" + item.DocumentTitle + "\")' class='' ><i class='fa fa-trash-o deletepopover'></i> </a>";   //data-toggle='modal' data-target='#formConfirm' data-title='Delete' data-message='Are you sure you want to delete this document ?'

                                count = oTable1.fnAddData([item.DocumentID, Link, (item.Category == "null" ? "" : item.Category), WhoHasAccess,
                              (item.CreatedDate == null ? '' : createdDate), (item.ValidFrom == null ? '' : validFrom),
                              (item.ValidUpto == null ? '' : validTo), Edit + "" + Delete + "" + Share]);
                            });

                        }
                        else {
                            if ('@ViewBag.errorMessage' != null && '@ViewBag.errorMessage' != "") {
                                NotifyMessage('@ViewBag.errorMessage', 'top-right', '1800', 'warning', 'fa-warning', true);
                            }
                        }
                    },
                    complete: function (data) {
                        $('[data-toggle="modal"]').tooltip();
                        navBool = true;
                    }
                })
            }
        }

        var connString = '';
        var hdnParentID = '';

        function GetConnectionString() {

            var ClientID = $('#ddlMasterClient :selected').val();

            $.ajax({
                url: '/Manager/GetConnectionString',
                type: 'GET',
                async: false,
                data: { clientID: ClientID },
                success: function (data) {
                    connString = data;
                }
            })
        }

        function backParent(parentID) {

            if (navBool == false)
                return false;
            if (navBool == true)
                navBool = false;

            $("#navigationmenu").html('');
            i = 0;
            var navT = navTitle.split('/');
            var newText = "";

            for (var p = 0 ; p <= navT.length - 3 ; p++) {
                newText += navT[p] + "/";
                var Folder = 'class="fa fa-folder-open folder-color"';
                $("#navigationmenu").append('<a href=# style="vertical-align:-webkit-baseline-middle;  margin:0px 0px 0px 5px;" onclick=Navigate(' + p + ',' + docID[p] + ') ><i ' + Folder + ' ></i>' + navT[p] + '/</a>').show()

            }
            i = p;
            navTitle = newText;

            for (var g = i; g < docID.length; g++)
                docID.splice(g, docID.length);

            if (i == 0)
                GoToHome(0)
            else
                GetParentIDByDocumentID(parentID);
        }

        function GetParentIDByDocumentID(documentID) {

            var ClientID = $('#ddlMasterClient :selected').val();
            GetConnectionString();
            var ConnectionString = connString == undefined ? null : connString;

            $.ajax({
                url: '/Document/GetParentIDByDocumentID',
                data: { DocumentID: documentID },
                type: 'Get',
                success: function (data) {

                    var data1 = JSON.parse(data);

                    if (data1.ParentId != null) {

                        OpenFolderDocuments(data1.ParentId, '/Document/GetFilesByParentID');
                        $("#hdnParentId").val(data1.ParentId);
                    }
                }
            })
        }

        function GoToHome(parentID) {
            docID = [];
            navTitle = '';
            nav = '';
            i = 0;
            $("#navigationmenu").html('').show();
            $('#hdnNavigation').html('').show();
            navTitle = "";
            $('#hdnParentId').val('');

            OpenFolderDocuments(parentID, '/Document/GetFilesByParentID');
        }
        var s = 0;
        $(document).ready(function () {
            if ('@ViewBag.MessageDoc' != null && '@ViewBag.MessageDoc' != "") {
                SuccessMessage('@ViewBag.MessageDoc');
            }


            if ('@ViewBag.ParentID' != null && '@ViewBag.ParentID' != "") {

                OpenFolderDocuments('@ViewBag.ParentID', '/Document/GetFilesByParentID')

                var navigate = '@ViewBag.Navigation';
                $('#hdnNavigation').val(navigate); //change
                var Folder = 'class="fa fa-folder-open folder-color"';
                $('#hdnParentId').val('@ViewBag.ParentID');
                var navSplit = navigate.split('/');

                for (var j = 0; j < navSplit.length - 1; j++) {
                    var documentId = navSplit[j].split(',')[0]
                    var documentTitle = navSplit[j].split(',')[1]

                    navTitle += documentTitle + '/';

                    $("#navigationmenu").append('<a href=# style="vertical-align:-webkit-baseline-middle;  margin:0px 0px 0px 5px;" onclick=Navigate(' + (s++) + ',' + documentId + ') ><i ' + Folder + ' ></i>' + documentTitle + '/</a>').show(); //+ '<span style="vertical-align:-webkit-baseline-middle;">/</span>'
                    docID.push(documentId);
                    i = j + 1;
                }
            }
            else {

                navBool = false;
                LoadDocuments('/Document/GetDocumentsData');
            }

            $("#btnSubmit").on('click', function () {

                if ($('#txtName-error').text() != "") {
                    return false;
                }
                var clientID = $('#ddlMasterClient :selected').val();

                if (clientID == "" || clientID == null || clientID == "undefined") {

                    var btn = $('#AddUserView');
                    btn.button('loading');
                    btn.css("cursor", "default").fadeTo(500, "none");
                    NotifyMessage("Please select the client", 'top-right', '1200', 'warning', 'fa-warning', true);
                    $('.modal-content').hide();
                    $('.modal-backdrop').css({ position: 'initial' });
                    $('.AddFolder').hide();
                    setTimeout(function () { btn.button('reset'); btn.css("cursor", "pointer") }, 1200);
                    return false;
                }
                if ($('#optFile').val() == "true") {
                    return true;
                }

                if ($('#optFile').val() == "false" && $('#txtUploadPath').val() == "") {

                    $('#MsgUploadError').show();
                    return false;
                }
                else {
                    $('#MsgUploadError').hide();
                }

                if ($("#chkvaliditycheck").is(':checked')) {

                    var dateFrom = $('#id-date-picker-1-txtValidFrom').val();
                    var dateTo = $('#id-date-picker-1-txtValidUpto').val();

                    if (dateFrom == "" && dateTo == "") {
                        $('#MsgValidFromError').show();
                        $('#MsgValidUptoError').show();

                        //Notify('Valid From and Valid Upto dates are required.', 'top-right', '3000', 'warning', 'fa-warning', true);
                        //window.setTimeout(function () { btn.button('reset'); }, 3000);

                        return false;
                    }
                    else {
                        $('#MsgValidFromError').hide();
                        $('#MsgValidUptoError').hide();
                    }


                    if (Date.parse(dateFrom) > Date.parse(dateTo)) {
                        $('#MsgValidUptoError').show();
                        $('#MsgValidUptoError').text('Valid Upto date should not be less than Valid From date.');

                        //Notify('Valid Upto date should not be less than Valid From date.', 'top-right', '3000', 'warning', 'fa-warning', true);
                        //window.setTimeout(function () { btn.button('reset'); }, 3000);

                        return false;
                    }
                    else {
                        $('#MsgValidUptoError').hide();
                    }
                }

                var returnBool = $("#chkvaliditycheck").is(':checked');

                if (returnBool) {

                    var ret;
                    if ($('#id-date-picker-1-txtValidFrom').val() == '') {
                        $('#MsgValidFromError').show();

                        //Notify('Valid From date is required', 'top-right', '3000', 'warning', 'fa-warning', true);
                        //window.setTimeout(function () { btn.button('reset'); }, 3000);

                        return false;
                    }
                    else {
                        $('#MsgValidFromError').hide();
                    }

                    if ($('#id-date-picker-1-txtValidUpto').val() == '') {
                        $('#MsgValidUptoError').show();

                        //Notify('Valid Upto date is required', 'top-right', '3000', 'warning', 'fa-warning', true); btn.button('reset');
                        //window.setTimeout(function () { btn.button('reset'); }, 3000);

                        return false;
                    }
                    else {
                        $('#MsgValidUptoError').hide();
                    }
                }
                //btn.button('reset');
            });
            $(".validitycheck").hide();
            $('input[type="checkbox"]').click(function () {
                if ($(this).attr("value") == "validitycheck") {
                    $(".validitycheck").toggle();
                }
            });
        });

    </script>
    <script>
        //--Bootstrap Date Picker--
        $('.date-picker').datepicker();

        $('.date-picker').on('changeDate', function (ev) {
            $(this).datepicker('hide');
        });

    </script>
    <script type="text/javascript">

        function GetUserNames() {
            var ClientID = $('#ddlMasterClient :selected').val();
            $('#txtShareTo').empty();

            $.ajax({
                url: '/Manager/GetAllUserNames',
                type: 'GET',
                success: function (data) {
                    var data1 = JSON.parse(data);
                    var list = '';

                    $.each(data1, function (i, v) {

                        list += "<option value=" + v.UserID + ">" + v.EmailAddress + "</option>";
                    })
                    $('#txtShareTo').html(list).show();
                }
            })
        }

        $(document).ready(function () {

            GetUserNames();
        });

    </script>
    <script type="text/javascript">

        $('#ddlMasterClient').on('change', function () {

            var ClientID = $("#ddlMasterClient :selected").val();
            var ClientName = $("#ddlMasterClient :selected").text();

            $.ajax({
                type: 'POST',
                url: '/Login/ChangeClientDetails',
                data: { DatabaseId: ClientID, ClientName: ClientName },
                beforeSend: function () {
                    $('.loading-container').show();
                    $('.loader').show();
                },
                success: function (data) {

                    LoadDocuments('/Document/GetDocumentsData');

                    $("#navigationmenu").html('');
                    $('#OpenFolder').hide();

                    GetUserNames();

                    $('#ShareFiles').slideUp();
                    $("#txtShareTo").val('');
                    $("#txtShareDesc").val('');

                },
                complete: function () {

                    $('[data-toggle="modal"]').tooltip();
                    $('.loading-container').hide();
                    $('.loader').hide();
                }
            });
        });

        $(document).ready(function () {

            $('#formAddFolder #txtName,#EditFolder #txtFolderName').keypress(function (event) {

                var keycode = (event.keyCode ? event.keyCode : event.which);
                if (keycode == '47') {
                    return false;
                }
            });
            $('#txtName,#txtFolderName').on('keypress', function (event) {

                var keycode = (event.keyCode ? event.keyCode : event.which);
                if (keycode == '44') {
                    return false;
                }
            });

            $('.page-sidebar').css('z-index', '2');
            $('.navbar').css('z-index', '200');

            $('#optFile').change(function () {

                if ($('#optFile').val() == "false") {
                    $('#divFileType').show();
                    $('.modal-body').find('.form-group').eq(2).show();
                    $('.modal-body').find('.form-group').eq(5).show();
                    $("#divValidity").show();
                }
                else {
                    $('#divFileType').hide();
                    $('.modal-body').find('.form-group').eq(2).hide();
                    $('.modal-body').find('.form-group').eq(5).hide();
                    $("#divValidity").hide();
                }
            });

            $('#btnCancelShare').click(function () {

                $('#ShareFiles').slideToggle();
                $('.select2-selection__rendered').empty();
                $("#txtShareDesc").val('');
                $('#chkType').attr('checked', false);
            });

            $('#btnCancelAdd').click(function () {

                $('.modal-body input').val('');
                $('#txtDescription').val('');
                $("#chkvaliditycheck").prop('checked', false).change();
                $("#divValidity").hide();
                $('#optFile option[value=true]').prop("selected", true).change();
                $('#txtName-error').empty();
                $('#txtDescription-error').empty();
            });

            $("#chkvaliditycheckEdit").change(function () {

                $('#MsgEditValidFrom').hide();
                $('#MsgEditValidUpto').hide();
                $(".validitycheck").hide();

                var chk = $(this).is(':checked');

                if (chk) {
                    $(".validitycheck").show();
                }
                else {
                    $('#id-date-picker-1-txtValidFromEdit').val('')
                    $('#id-date-picker-1-txtValidUptoEdit').val('')
                }
            });

            $("#chkvaliditycheck").change(function () {
                $(".validitycheck").hide();
                var chk = $(this).is(':checked');

                if (chk) {
                    $(".validitycheck").show();
                }
                else {
                    $('#id-date-picker-1-txtValidFrom').val('')
                    $('#id-date-picker-1-txtValidUpto').val('')
                }
            });

            $("[data-toggle=tooltip]").tooltip();
            $(".upload").hide();

            $('input[type="radio"]').click(function () {

                if ($(this).attr("value") == "createnew") {
                    $(".box").not(".createnew").hide();
                    $(".createnew").show();
                }

                if ($(this).attr("value") == "upload") {
                    $(".box").not(".upload").hide();
                    $(".upload").show();
                }
            });

            $('#btnAddFileorFolder').click(function () {

                var navigationmenu = null;//$('#hdnNavigation').text();
                $('#hdnNavigation').val(navigationmenu);

                if (navigationmenu == null || navigationmenu == "") {

                    var title = navTitle.split('/');
                    $('#hdnNavigation').html('');
                    var navigateNew = '';

                    for (var s = 0; s <= title.length - 2; s++) {
                        navigateNew += docID[s] + ',' + title[s] + '/';
                    }

                    $('#hdnNavigation').val(navigateNew);
                }

                var parentID = $('#hdnParentId').val();
                $('#hdnParent').val(parentID);
                $('.modal-content').show();
                $('#ShareFiles').hide();
                $("#OpenFolder").hide();
                $('#txtName,.form-control#txtDescription,#id-date-picker-1-txtValidFrom,#id-date-picker-1-txtValidUpto,#txtUploadPath').val('');
                $('#chkvaliditycheck').attr('checked', false);
                $('#optFile option[value=true]').prop("selected", true).change();
                $(".validitycheck").hide();
                $('.page-sidebar').css('z-index', '2');
                $('.navbar').css('z-index', '200');

            });

            $('.editpopover').click(function () {
                $('.page-sidebar').css('z-index', '0');
                $('.navbar').css('z-index', '200');
            });

            $('.deletepopover').click(function () {
                $('.page-sidebar').css('z-index', '0');
                $('.navbar').css('z-index', '200');
            });
        });

        function ClearAdd() {
            $('.select2-selection__rendered').empty();
            $('.modal-body input').val('');
            $('#txtDescription').val('');
            $("#chkvaliditycheck").prop('checked', false).change();
            $("#divValidity").hide();
            $('#optFile option[value=true]').prop("selected", true).change();
            $('#txtName-error').empty();
            $('#txtDescription-error').empty();

            $('#MsgUploadError').hide();
            $('#MsgValidFromError').hide();
            $('#MsgValidUptoError').hide();
        }

        function NotifyMessage(message, position, timeout, theme, icon, closable) {
            toastr.options.positionClass = 'toast-' + position;
            toastr.options.extendedTimeOut = 0; //1000;
            toastr.options.timeOut = timeout;
            toastr.options.closeButton = closable;
            toastr.options.iconClass = icon + ' toast-' + theme;
            toastr['custom'](message);
        }

        function ClearShare(documentId) {
            $('#hdnDocumentId').val(documentId)
            $("#txtShareTo").val('');
            $("#txtShareDesc").val('');

            $('#chkType').attr('checked', false);
        }

        function DeleteShare(shareId, shareToUser) {
            var ClientID = $('#ddlMasterClient :selected').val();

            bootbox.confirm({
                title: "Delete",
                message: "Are you sure you want to delete?",
                buttons: {
                    cancel: {
                        label: '<i class="fa fa-times"></i> Cancel'
                    },
                    confirm: {
                        label: '<i class="fa fa-check"></i> Confirm'
                    }
                }, callback: function (result) {
                    if (result) {
                        $.ajax({
                            url: '/Document/DeleteDocumentShareById',
                            data: { documentShareId: shareId, clientID: ClientID },
                            type: 'Post',
                            success: function (data) {
                                SuccessMessage("Shared user '" + shareToUser + "' has been deleted successfully.");
                                // Notify("Shared user " + '"' + shareToUser + '"' + " has been deleted successfully", 'top-right', '2000', 'success', 'fa-check', true);
                                var id = $('#hdnDocumentId').val();
                                AccessDocument123(id);
                            }
                        });
                    }
                }
            });
        }

        function Downloadfile(id) {

            var ClientID = $('#ddlMasterClient :selected').val();

            $.ajax({
                url: '/Document/GetDocumentsForDownload',
                type: 'GET',
                data: { documentId: id, clientID: ClientID },
                success: function (data) {

                    var data1 = JSON.parse(data)
                    if (data1.UploadDocument != null && data1.UploadDocument != "") {

                        var url = "/Document/Download?documentId=" + id + "&&clientID=" + ClientID;
                        window.location = url;
                    }
                    else {
                        Notify('There is no file to download.', 'top-right', '900', 'warning', 'fa-warning', true);
                        return false;
                    }
                }
            });
        }

    </script>
    <script>
        $("#txtShareTo").select2({
            placeholder: "Select a user",
            allowClear: true
        });
    </script>*@

</environment>